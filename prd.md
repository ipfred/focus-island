这份产品需求文档（PRD）为你构思的**“番茄灵动岛（Pomodoro Island）”**进行了系统化的梳理。为了解决“鼠标靠近自动躲避”与“需要用鼠标控制番茄钟”之间的交互冲突，我在文档中引入了**“全局快捷键”**和**“交互意图判定”**机制。

---

# 🍅 番茄灵动岛 (Pomodoro Island) - 产品需求文档 (PRD) v1.0

## 一、 产品概述
* **产品愿景**：成为桌面端最轻量、最不具侵入性，但又最能有效聚拢注意力的护城河工具。
* **核心价值**：通过“灵动岛”形态的桌面悬浮窗，结合番茄工作法与智能防干扰机制，解决办公时注意力被网页/社交软件轻易转移的痛点。
* **目标平台**：Windows, macOS (基于 Tauri / 跨平台低占用方案)。

---

## 二、 核心功能需求

### 1. 任务管理模块 (Task List)
* **任务清单管理**：支持快速添加、修改、删除任务，标记任务完成。
* **轮播展示（待机态）**：在未开启番茄钟时，灵动岛以一定时间间隔（如每10秒）轮番显示最近需要处理的**3个任务**（1个主任务，2个次任务）。
* **番茄记录**：每个任务左侧或右侧显示小圆点/数字，记录该任务已经完成的番茄钟次数。

### 2. 番茄时钟模块 (Pomodoro Timer)
* **快速启动**：点击任务清单中的任意任务，立即进入“专注态”（番茄钟倒计时开始）。
* **视觉呈现**：
  * 灵动岛中央显示当前专注的主任务名称。
  * 灵动岛边框化身为**进度条**（如逐渐闭合的流光光圈）。
  * 带有微弱的动态流光效果，给予“时间正在流逝”的潜意识提醒。
* **快捷控制（核心痛点解决）**：
  * **暂停/继续**：随时暂停当前番茄钟。
  * **提前结束（进入休息）**：当前任务提前做完，直接切入休息倒计时。
  * **跳过休息（进入工作）**：感觉状态好，随时结束休息，开启下一个番茄。
  * **放弃番茄**：终止当前记录。

### 3. 智能防干扰与交互模块 (Smart Avoidance)
* **智能闪避与穿透（Click-through）**：
  * 当鼠标指针靠近灵动岛（防遮挡网页标签栏等）：灵动岛自动触发“闪避态”，**缩变为贴着屏幕边缘的一条“细线”**。
  * 细线状态下依然通过颜色/长度显示番茄钟进度。
  * 细线状态默认开启**鼠标穿透**，不影响用户点击背后的浏览器标签或软件按钮。
* **交互意图判定（如何操作它？）**：
  * 既然靠近会躲开，如何点击“暂停”？
  * **方案A（全局快捷键）**：按 `Alt + P` 暂停，`Alt + S` 跳过。
  * **方案B（意图唤醒）**：按住 `Ctrl/Cmd` 键时，鼠标靠近灵动岛，灵动岛**不躲避**，而是展开为“控制面板”（显示播放、暂停、跳过等按钮）。
  * **方案c（右键唤醒）**：右键点击边缘的细线，弹出番茄钟的控制面板

### 4. 主动防疲劳机制 (Anti-Fatigue & Focus Retention)
* **动态微交互（破除视觉盲区）**：
  * **呼吸闪烁**：专注时间每进行到 15 分钟/30分钟，灵动岛会有一次温柔的“呼吸放大”或边框颜色渐变，把游离的余光拉回来。
* **闲置灵魂拷问（Idle Detection）**：
  * 侦测系统全局键鼠活动。如果用户长时间未操作鼠标键盘（例如超过 5 分钟），灵动岛高亮变为醒目的颜色（如橙色）。
  * 弹出文字：“**还在做 [当前任务] 吗？**”
  * 用户移动鼠标到悬灵动岛后或者敲击键盘后，恢复正常状态。

### 5. 个性化设置模块 (Settings)
* **位置与多屏**：支持吸附在屏幕的 上、下、左、右 边缘。支持在多显示器环境下选择常驻屏幕。
* **尺寸调节**：支持调节灵动岛的长度、宽度、字体大小。
* **时间设定**：自定义专注时长（默认25分）和休息时长（默认5分）。

---

## 三、 状态机设计 (UI 状态转化)

为了让“灵动岛”名副其实，需要设计流畅的状态切换动画：

| 状态名称 | 触发条件 | UI 表现形态 |
| :--- | :--- | :--- |
| **待机态 (Idle)** | 无番茄钟进行时 | 正常胶囊大小，文字轮播最近3个任务，无进度条。 |
| **专注态 (Focus)** | 开启番茄钟 | 锁定显示主任务，边框显示流光进度条。 |
| **闪避态 (Hide)** | 鼠标靠近且未按唤醒键 | 极速收缩为屏幕边缘的 2px 细线（颜色代表进度），鼠标穿透。 |
| **控制态 (Control)** | 按住 `Ctrl` 键且鼠标靠近 | 胶囊向下/向侧边展开，露出 暂停、停止、跳过 等操作按钮。 |
| **唤醒态 (Alert)** | 专注达20分钟 / 闲置5分钟 | 胶囊轻微震动/呼吸，颜色变化，显示提示文字。 |

---

## 四、 非功能性需求 (技术与性能)

1. **极低性能占用 (内存 < 50MB, CPU 几乎为 0)**
   * **强烈建议技术栈：Tauri + Vue3/React + TailwindCSS**。
   * *理由*：Electron 无论怎么优化，启动后基本都会吃掉 100MB+ 内存，且进程较多。Tauri 基于 Rust 和系统原生 Webview，打包体积仅几 MB，内存占用极低，非常适合这种永远挂在后台的极简小工具。
2. **系统级 API 调用**：
   * 需要调用系统级 API 实现：**窗口置顶 (Always on top)**、**鼠标穿透 (Ignore mouse events)**、**全局快捷键 (Global Shortcuts)**、**全局闲置状态侦听 (System Idle Time)**。
3. **数据存储**：
   * 本地优先 (Local-First)。任务数据和番茄历史记录存储在本地 SQLite 或 JSON 文件中，无需强制联网。

---

## 五、 开发实施建议（MVP版本规划）

为了避免陷入“开发一整年，上线没人用”的陷阱，建议分阶段开发：

* **v0.1 核心闭环版**：
  * 只做屏幕顶部的居中灵动岛（固定位置，不考虑多屏和四边）。
  * 实现：任务输入 -> 开启25分钟倒计时 -> 边框进度条缩短 -> 时间到提示。
  * 实现鼠标靠近缩小为细线。
* **v0.5 交互完善版**：
  * 引入全局快捷键控制暂停/跳过。
  * 引入每 15 分钟的“呼吸”微交互提醒。
  * 引入系统空闲检测。
* **v1.0 正式发布版**：
  * 开放设置页（自定义时间、位置、多屏选择）。
  * 完善任务的CRUD（增删改查）和番茄数据统计。

---

**开发者提示**：这个设计的核心难点在于 **“闪避（鼠标穿透）”与“控制（点击按钮）”的冲突**。一定要在早期技术调研阶段，先写一个 Demo 测试 Tauri 或 Electron 的 `setIgnoreMouseEvents` API，确保能够做到：**根据鼠标位置动态开启和关闭鼠标穿透**，这是“番茄灵动岛”最核心的爽点。