这是一个非常经典的**“极简交互”与“功能扩展”之间的矛盾**。

我先给出我的明确结论：**我更推荐方案 2（主窗口 + 灵动岛），但必须在“形态”上做文章，让主窗口看起来像是灵动岛的“延伸”，而不是两个割裂的东西。**

下面我为你详细拆解这两个方案的**成熟解决思路、交互设计和在 Tauri 中的技术实现方式**，帮你打通这个纠结。

---

### 方案一：纯粹的“单灵动岛”方案（Single Island）

这个方案最炫酷，所有的操作都在这一个“胶囊”里完成。
**核心冲突点**：既然鼠标靠近它要“躲开并允许鼠标穿透”，那我怎么去点击“新增任务”和“暂停”呢？

#### 💡 成熟的解决方案设计：快捷键唤醒 + 状态形态演变

我们可以参考 Mac 上的 **Raycast / Spotlight** 以及苹果灵动岛的真实交互逻辑，放弃用鼠标去“找”灵动岛，而是**用快捷键让灵动岛变成控制台**。

*   **平时（专注态/防遮挡态）**：灵动岛是一条线或小胶囊。开启 `setIgnoreMouseEvents(true)`（鼠标穿透），鼠标滑过毫无阻碍。
*   **交互（控制态）**：
    *   用户按下全局快捷键（如 `Alt + Space` 或 `Ctrl + Shift + X`）。
    *   灵动岛瞬间**居中并向下展开**，变成一个类似搜索框的控制台。此时关闭鼠标穿透 `setIgnoreMouseEvents(false)`。
    *   **如何新增任务**：直接在这个框里打字，按回车，自动加入任务并开始番茄钟。
    *   **如何暂停/跳过**：展开状态下，会显示出 ⏸️ 和 ⏭️ 按钮，用鼠标点击或按 `Tab` 切换。
    *   按 `Esc` 或鼠标点击屏幕其他地方（失去焦点 Blur），灵动岛立刻缩回去，再次开启鼠标穿透。

#### 🛠 Tauri 技术实现要点（方案一）
1.  **单窗口**：只创建一个无边框透明窗口（Frameless Transparent Window）。
2.  **动态穿透**：
    ```javascript
    // Tauri 前端调用示例
    import { appWindow } from '@tauri-apps/api/window';
    // 正常状态：开启鼠标穿透
    await appWindow.setIgnoreMouseEvents(true);
    // 按下快捷键展开后：关闭鼠标穿透，允许点击
    await appWindow.setIgnoreMouseEvents(false);
    ```
3.  **动画引擎**：由于窗口大小其实是固定的（包含展开后的最大尺寸），只是里面的 HTML 元素在变化，建议用 Vue 的 `<Transition>` 或 Tailwind 的 `transition-all` 做元素的放大缩小动画。

---

### 方案二：任务面板 + 灵动岛（Dashboard + Island）—— ⭐️ 强烈推荐

你担心“不太像一个灵动岛产品”，是因为你脑海里的“任务窗口”可能是一个带有最小化、关闭按钮的常规软件窗口。
**只要把任务窗口做成“非模态面板（Overlay Panel）”或“抽屉”，就不会破坏灵动岛的沉浸感。**

#### 💡 成熟的解决方案设计：母舰与侦察机模式

*   **灵动岛（侦察机）**：永远置顶在屏幕顶部边缘。
    *   **职责**：只负责**显示进度**、**提醒当前任务**、**防疲劳微交互**。
    *   **交互**：完全鼠标穿透，靠近自动变成线。**完全不需要在它身上做任何点击操作**。
*   **任务面板（母舰）**：
    *   **形态**：它不是一个常规窗口，而是一个**无边框、带有磨砂玻璃效果（毛玻璃）的悬浮面板**。
    *   **呼出方式**：可以通过系统托盘（右下角/右上角图标）点击呼出，或者按快捷键呼出。
    *   **位置**：呼出时，从灵动岛的正下方“滑出”（视觉上让人觉得它是从灵动岛变出来的）。
    *   **操作**：在这里进行列表管理、查看统计数据、设置界面。点击某个任务的“开始”按钮后，面板自动隐藏，灵动岛开始工作。

#### 🛠 Tauri 技术实现要点（方案二）
你需要创建**两个 Tauri Window**：
1.  **Window 1 (Island)**: `alwaysOnTop: true`, `transparent: true`, `decorations: false`。长期存活，永远执行 `setIgnoreMouseEvents(true)`。
2.  **Window 2 (Dashboard)**: `decorations: false`, `transparent: true` (用来做毛玻璃效果)。
    *   监听失去焦点事件（`tauri::WindowEvent::Focused(false)`），一旦用户点击了浏览器或写代码的IDE，Dashboard 窗口立刻隐藏（`hide()`），做到用完即走。
3.  **进程间通信 (IPC)**：Dashboard 窗口点击了“开始任务”，通过 Tauri 的 `emit` 给后台发消息，后台再转发给 Island 窗口更新 UI。

---

### 总结与最终建议

**为什么我强烈推荐【方案二（改良版）】？**

1.  **开发难度低，坑少**：方案一频繁切换鼠标穿透，并在一个极小窗口里做输入框、按钮切换，前端 CSS 布局和系统兼容性（Windows和Mac对透明窗口重绘的处理不同）很容易出 Bug。方案二各司其职，逻辑清晰。
2.  **后期扩展性无敌**：你现在觉得只需要三个任务。等你自己用了一周，你一定会想：“我要不要加个历史番茄钟统计图表？”“我要不要加个月度回顾？”。方案一的单灵动岛根本塞不下这些，最后还是得开新窗口。方案二的 Dashboard 可以无限扩展。
3.  **符合用户直觉**：参考 Mac 上优秀的效率软件（如 TickTick/滴答清单），它们都是“一个主面板管理 + 一个极其克制的顶部悬浮条展示进度”。

**最终产品形态描绘**：
平时，屏幕顶部只有一个极小的胶囊显示：“当前：写 PRD 文档”。你鼠标滑过，它缩成一条线，不挡你看网页。
你想切任务了，按下 `Alt + P`，屏幕中间弹出一个毛玻璃质感的极简面板（Dashboard），你输入新任务，敲回车，面板瞬间消失，顶部的灵动岛变成了新任务的名字。
完美，极其优雅，而且绝对“灵动”。


---

## 任务清单页面布局

为“番茄灵动岛”设计任务面板（Dashboard），核心原则是：**“呼之即来，挥之即去，绝不成为新的注意力黑洞”**。

它不能像传统的 Todo 应用（如滴答清单、Todoist）那样拥有复杂的侧边栏、日历视图、项目文件夹。它应该更像 **Raycast、Spotlight 或 Mac 上的 Things 3 的快捷录入框** —— 轻量、键盘驱动、极简。

以下是为你量身定制的任务面板功能与界面布局方案：

---

### 一、 核心功能规划 (Feature List)

保持极简，只保留与“当下专注”最强相关的功能：

1. **闪电录入 (Quick Add)**：打开面板直接就是输入框，打字回车，立刻变成任务。
2. **WIP 限制区 (Focus Queue)**：完美还原你最初的构想，严格划出 **1个主任务 + 2个次级任务** 的专属展示区。
3. **番茄钟操控 (Timer Controls)**：针对当前选中的任务，一键开启、暂停、完成番茄钟。
4. **脑暴收件箱 (Inbox/Backlog)**：用于存放突然想到但现在不做的事，防止遗忘，但不占用前 3 的宝贵名额。
5. **微型成就看板 (Micro Stats)**：仅展示“今日已完成番茄数”和“今日已完成任务”，提供轻量级的正反馈。

---

### 二、 界面布局设计 (UI Layout Blueprint)

面板整体风格：**无边框 (Frameless) + 圆角 + 毛玻璃背景 (Acrylic/Blur)**。
默认尺寸建议：宽 `400px`，高 `500px` 左右，居中弹出。

你可以将界面分为 **四个横向区域**，以下是结构示意图：

```text
╭────────────────────────────────────────╮
│                                        │
│  [Zone 1: 全局输入区]                   │
│  ⌨️ 接下来专注做什么...        [Enter ↵] │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  [Zone 2: 核心专注区 (Top 3 限制)]       │
│                                        │
│  👑 主任务 (正在进行 / 待开启)           │
│  [⭕️] 编写番茄灵动岛PRD文档       ▶️ 🍅x2 │
│                                        │
│  🥈 次级任务 (准备接替)                 │
│  [⭕️] 调研 Tauri 鼠标穿透 API    ▶️ 🍅x0 │
│  [⭕️] 回复张总的邮件             ▶️ 🍅x0 │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  [Zone 3: 灵感收件箱 (可滚动列表)]       │
│  📥 稍后处理 (4)                        │
│   · 下午去拿快递                        │
│   · 整理桌面文件                        │
│   · 订明天的机票                        │
│   · ...                                │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  [Zone 4: 状态与设置底栏]                │
│  🍅 今日专注: 6 个 (约150分钟)       [⚙️] │
│                                        │
╰────────────────────────────────────────╯
```

---

### 三、 各区域交互细节 (Zone Details)

#### Zone 1: 全局输入区 (Command Input)
* **交互**：面板呼出时，**光标永远默认聚焦在这里**。
* **逻辑**：
  * 输入文字后按 `Enter`，如果 Zone 2（Top 3区）没满，自动进入 Top 3；满了则自动落入 Zone 3（收件箱）。
  * 输入文字后按 `Shift + Enter`，不仅创建任务，而且**瞬间关闭面板，顶部灵动岛直接开始番茄钟倒计时**（丝滑连招）。

#### Zone 2: 核心专注区 (Focus Queue - 你的核心痛点)
* **视觉**：这部分 UI 背景色可以稍微提亮，或者加上卡片阴影，与下方收件箱区分开。主任务（👑）字号大一号，加粗。
* **操作**：
  * 鼠标悬停在任务上，右侧浮现 `▶️ (开始番茄)` 和 `🗑️ (删除)` 按钮。
  * **拖拽排序 (Drag & Drop)**：按住次级任务拖到主任务位置，即可完成替换。
  * 左侧的 `[⭕️]` 是复选框，点击直接标记完成，完成时划上删除线并在一秒后消失，下方的任务自动递补上来。

#### Zone 3: 灵感收件箱 (Inbox)
* **痛点解决**：你在专注时，突然想到“卧槽，忘了拿快递”。此时按快捷键呼出面板，在 Zone 1 飞速输入“拿快递”按回车，它落入 Zone 3。你按 `Esc` 关掉面板继续工作。**不打断当前番茄钟，但清空了大脑内存**。
* **视觉**：字体稍小，颜色偏灰，降低视觉权重。

#### Zone 4: 状态底栏 (Footer)
* **左侧**：显示今天的“战绩”（番茄数量）。点击可以弹出一个小小的弹窗，显示本周的柱状图（后续迭代考虑）。
* **右侧**：设置按钮（⚙️）。点击翻转到面板背面（或弹窗），设置番茄时长（25/5）、灵动岛的位置、快捷键绑定等。

---

### 四、 状态切换：当番茄钟正在运行时

如果你已经开启了一个番茄钟，此时按快捷键呼出面板，**Zone 2 的 UI 应该发生变化**，变成“控制台”模式：

```text
  [Zone 2: 核心专注区 - 运行态]       
                                        
  ⏱️ 正在专注：23:14                     
  编写番茄灵动岛PRD文档                  
                                        
  [ ⏸️ 暂停 ]  [ ⏹️ 提前完成 ]  [ ⏭️ 放弃 ] 
```
这样不仅顶部灵动岛能看进度，主面板也能提供清晰的大按钮供你操作。

---

### 五、 极客体验加分项 (Tauri 开发建议)

1. **纯键盘操作 (Keyboard Navigation)**：
   让懂行的用户爱不释手的设计：面板打开后，按 `↑` `↓` 键在任务列表间穿梭，按 `Space` 标记完成，按 `Enter` 开始番茄钟。全程不需要碰鼠标。
2. **全局快捷键呼出 (Global Shortcut)**：
   借助 Tauri 的 `globalShortcut` API，注册类似 `Alt + Space` 或 `Ctrl + Shift + A` 的快捷键。无论你在浏览器还是写代码，一按就弹出面板，再按（或失去焦点）就隐藏。
3. **沉浸式毛玻璃 (Vibrancy / Mica)**：
   * 在 macOS 上，使用 Tauri 插件 `window-vibrancy` 开启原生毛玻璃效果。
   * 在 Windows 11 上，开启 `Mica` 材质背景。这会让你的面板看起来像系统原生应用一样高级，彻底摆脱“网页打包套壳”的廉价感。

这个方案既保留了你“WIP 限制（最多3个任务）”的精髓，又提供了一个符合直觉的收纳和管理空间，配合顶部的灵动岛，绝对是一个杀手级的效率工具组合！